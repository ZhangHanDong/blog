<h1><%= h(@comments.first.post.blog.title) unless @comments.empty? %> comments <%= "#{ "on #{link_to h(@post.title), url_for(@post.permalink_url)}" if @post }#{ "by #{h(@user.name)}" if @user }" %></h1>     

<% unless @comments.empty? %>  
  <%= render :partial => 'comments/comment', :collection => @comments -%>
<% end %>      
              
<p><%= will_paginate @comments %></p>