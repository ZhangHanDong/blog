<%= f.error_messages %>

<p>
  <%= f.label :name %><br />
  <%= f.text_field :name, :value => (current_user.name if label != 'Update' && current_user) %>
</p> 
<p>
  <%= f.label :email %><br />
  <%= f.text_field :email, :value => (current_user.email if label != 'Update' && current_user) %>
</p>
<p>
  <%= f.label :website %><br />
  <%= f.text_field :website %>
</p>
<p>
  <%= f.label :body %><br />
  <%= f.text_area :body %>  
</p>
<p>
   <%= f.submit label %>   
</p>