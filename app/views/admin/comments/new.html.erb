<h1>New comment (for post, <strong><%= h @post.title %></strong>)</h1>

<% form_for [:admin, @post, @comment] do |f| %>
  <%= render :partial => 'form', :locals => { :f => f, :label => 'Create' } -%>
<% end %>

<%= link_to 'Back', admin_post_comments_path(@post) %>
